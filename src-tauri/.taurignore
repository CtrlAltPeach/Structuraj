# Файлы, которые не должны запускать пересборку в `tauri dev`.
# Иконки попадают в бандл на этапе `tauri build`, а во время разработки
# перегенерация набора через `tauri icon` иначе вызывает десятки пересборок подряд.
icons/
gen/
tests/samples/
